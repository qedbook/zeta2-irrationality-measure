/-
# Row PT-P, layer 12 — `KummerRowsOpen`'s stratum lemmas, shard 1 of 4: strata 0–26

GENERATED by `gen_kummer_lean.py` from `ptp_kummer_strata.py`'s strata; do not edit by hand.
Each lemma quantifies over EVERY `(p, r₀, ρ, u)` with `r₀/p` in its stratum, in the vocabulary
of `Zeta2PtpKumRes` (`targetR`, `kumLoR`, `kumHiR`), and is proved by `residue_spec` with the
stratum's literal floors, the `%` rewrites of `Zeta2PtpKumRes`/`Zeta2PtpCong`, and `omega`.
Nothing here is a valuation bound; `Zeta2Target.zeta2_not_liouvilleWith` is `sorry`.

VINTAGE: toolchain leanprover/lean4:v4.34.0-rc2, mathlib 5aedf732 (current), buildbox.
-/
import Zeta2Defs
import Zeta2Arith
import Zeta2QnInt
import Zeta2PnHarm
import Zeta2Profile
import Zeta2PhiT
import Zeta2NewtonCarry
import Zeta2NewtonAssemble
import Zeta2PnCleared
import Zeta2PnFunc
import Zeta2PnHarmDelta
import Zeta2PhiTDvd
import Zeta2PtpPolar
import Zeta2PtpRun
import Zeta2PtpStratum
import Zeta2PtpCong
import Zeta2PtpPoly
import Zeta2PtpCoeff
import Zeta2HatCarry
import Zeta2PtpKummer
import Zeta2PtpKumRes

namespace Zeta2PtpKumS1

open Zeta2PtpRun Zeta2PtpCong Zeta2PtpKumRes

-- Every stratum lemma carries the SAME binder list so the cover chain can apply it positionally;
-- on a trivial-target stratum `hu`/`hρ1`/`hρ` are then unused.
set_option linter.unusedVariables false

/-- stratum 0: `[0, 1/26)`, φ̃ = 0. -/
theorem lo_S0 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 0 * p ≤ 1 * r) (hhi : 26 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 0: `[0, 1/26)`, φ̃ = 0, the terms above `26n`. -/
theorem hi_S0 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 0 * p ≤ 1 * r) (hhi : 26 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 0: `[0, 1/26)`, φ̃ = 0. -/
theorem pol_S0 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 0 * p ≤ 1 * r) (hhi : 26 * r < 1 * p) :
    0 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 0) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 0) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 0) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 0) (a := 22 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 0: `[0, 1/26)`, φ̃ = 0, the terms above `26n`. -/
theorem polh_S0 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 0 * p ≤ 1 * r) (hhi : 26 * r < 1 * p) :
    0 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 0) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 0) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 0) (a := 17 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 1: `[1/26, 1/24)`, φ̃ = 0. -/
theorem lo_S1 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 26 * r) (hhi : 24 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 1: `[1/26, 1/24)`, φ̃ = 0, the terms above `26n`. -/
theorem hi_S1 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 26 * r) (hhi : 24 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 1: `[1/26, 1/24)`, φ̃ = 0. -/
theorem pol_S1 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 26 * r) (hhi : 24 * r < 1 * p) :
    0 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 0) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 1) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 0) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 0) (a := 22 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 1: `[1/26, 1/24)`, φ̃ = 0, the terms above `26n`. -/
theorem polh_S1 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 26 * r) (hhi : 24 * r < 1 * p) :
    0 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 0) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 0) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 0) (a := 17 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 2: `[1/24, 1/22)`, φ̃ = 0. -/
theorem lo_S2 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 24 * r) (hhi : 22 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 2: `[1/24, 1/22)`, φ̃ = 0, the terms above `26n`. -/
theorem hi_S2 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 24 * r) (hhi : 22 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 2: `[1/24, 1/22)`, φ̃ = 0. -/
theorem pol_S2 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 24 * r) (hhi : 22 * r < 1 * p) :
    0 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 0) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 1) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 1) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 0) (a := 22 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 2: `[1/24, 1/22)`, φ̃ = 0, the terms above `26n`. -/
theorem polh_S2 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 24 * r) (hhi : 22 * r < 1 * p) :
    0 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 0) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 0) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 0) (a := 17 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 3: `[1/22, 1/17)`, φ̃ = 0. -/
theorem lo_S3 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 22 * r) (hhi : 17 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 3: `[1/22, 1/17)`, φ̃ = 0, the terms above `26n`. -/
theorem hi_S3 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 22 * r) (hhi : 17 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 3: `[1/22, 1/17)`, φ̃ = 0. -/
theorem pol_S3 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 22 * r) (hhi : 17 * r < 1 * p) :
    0 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 0) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 1) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 1) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 1) (a := 22 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 3: `[1/22, 1/17)`, φ̃ = 0, the terms above `26n`. -/
theorem polh_S3 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 22 * r) (hhi : 17 * r < 1 * p) :
    0 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 0) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 0) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 0) (a := 17 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 4: `[1/17, 1/15)`, φ̃ = 0. -/
theorem lo_S4 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 17 * r) (hhi : 15 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 4: `[1/17, 1/15)`, φ̃ = 0, the terms above `26n`. -/
theorem hi_S4 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 17 * r) (hhi : 15 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 4: `[1/17, 1/15)`, φ̃ = 0. -/
theorem pol_S4 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 17 * r) (hhi : 15 * r < 1 * p) :
    0 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 0) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 1) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 1) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 1) (a := 22 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 4: `[1/17, 1/15)`, φ̃ = 0, the terms above `26n`. -/
theorem polh_S4 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 17 * r) (hhi : 15 * r < 1 * p) :
    0 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 0) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 0) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 1) (a := 17 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

set_option maxHeartbeats 2000000 in
/-- stratum 5: `[1/15, 1/13)`, φ̃ = 1. -/
theorem lo_S5 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 15 * r) (hhi : 13 * r < 1 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 0) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 1) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 1) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 1) (a := 22 * r) hp (by omega) (by omega)
  unfold targetR kumLoR Mres cb
  rw [hR11, hR13, hR9, hR5, hR26, hR24, hR22,
    add_mod_split hR11b (by omega : ρ ≤ p),
    wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 5: `[1/15, 1/13)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S5 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 15 * r) (hhi : 13 * r < 1 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 0) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 1) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 1) (a := 17 * r) hp (by omega) (by omega)
  unfold targetR kumHiR Mres cb
  rw [hR11, hR13, hR9, hR5, hR15, hR17,
    add_mod_split hR11b (by omega : ρ ≤ p),
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 5: `[1/15, 1/13)`, φ̃ = 1. -/
theorem pol_S5 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 15 * r) (hhi : 13 * r < 1 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 0) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 1) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 1) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 1) (a := 22 * r) hp (by omega) (by omega)
  unfold kumLoR Mres cb
  rw [hR11, hR13, hR9, hR5, hR26, hR24, hR22]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 5: `[1/15, 1/13)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S5 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 15 * r) (hhi : 13 * r < 1 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 0) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 1) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 1) (a := 17 * r) hp (by omega) (by omega)
  unfold kumHiR Mres cb
  rw [hR11, hR13, hR9, hR5, hR15, hR17]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
  split_ifs <;> omega

/-- stratum 6: `[1/13, 1/12)`, φ̃ = 0. -/
theorem lo_S6 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 13 * r) (hhi : 12 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 6: `[1/13, 1/12)`, φ̃ = 0, the terms above `26n`. -/
theorem hi_S6 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 13 * r) (hhi : 12 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 6: `[1/13, 1/12)`, φ̃ = 0. -/
theorem pol_S6 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 13 * r) (hhi : 12 * r < 1 * p) :
    0 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 2) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 1) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 1) (a := 22 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 6: `[1/13, 1/12)`, φ̃ = 0, the terms above `26n`. -/
theorem polh_S6 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 13 * r) (hhi : 12 * r < 1 * p) :
    0 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 1) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 1) (a := 17 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 7: `[1/12, 1/11)`, φ̃ = 0. -/
theorem lo_S7 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 12 * r) (hhi : 11 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 7: `[1/12, 1/11)`, φ̃ = 0, the terms above `26n`. -/
theorem hi_S7 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 12 * r) (hhi : 11 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 7: `[1/12, 1/11)`, φ̃ = 0. -/
theorem pol_S7 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 12 * r) (hhi : 11 * r < 1 * p) :
    0 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 2) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 2) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 1) (a := 22 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 7: `[1/12, 1/11)`, φ̃ = 0, the terms above `26n`. -/
theorem polh_S7 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 12 * r) (hhi : 11 * r < 1 * p) :
    0 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 0) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 1) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 1) (a := 17 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 8: `[1/11, 1/9)`, φ̃ = 2. -/
theorem lo_S8 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 11 * r) (hhi : 9 * r < 1 * p) :
    targetR p r ρ 2 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 2) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 2) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 2) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 8: `[1/11, 1/9)`, φ̃ = 2, the terms above `26n`. -/
theorem hi_S8 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 11 * r) (hhi : 9 * r < 1 * p) :
    targetR p r ρ 2 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 1) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 1) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

set_option maxHeartbeats 2000000 in
/-- stratum 8: `[1/11, 1/9)`, φ̃ = 2. -/
theorem pol_S8 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 11 * r) (hhi : 9 * r < 1 * p) :
    2 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 2) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 2) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 2) (a := 22 * r) hp (by omega) (by omega)
  unfold kumLoR Mres cb
  rw [hR11, hR13, hR9, hR5, hR26, hR24, hR22]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 8: `[1/11, 1/9)`, φ̃ = 2, the terms above `26n`. -/
theorem polh_S8 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 11 * r) (hhi : 9 * r < 1 * p) :
    2 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 0) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 1) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 1) (a := 17 * r) hp (by omega) (by omega)
  unfold kumHiR Mres cb
  rw [hR11, hR13, hR9, hR5, hR15, hR17]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
  split_ifs <;> omega

/-- stratum 9: `[1/9, 3/26)`, φ̃ = 2. -/
theorem lo_S9 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 9 * r) (hhi : 26 * r < 3 * p) :
    targetR p r ρ 2 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 2) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 2) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 2) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 9: `[1/9, 3/26)`, φ̃ = 2, the terms above `26n`. -/
theorem hi_S9 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 9 * r) (hhi : 26 * r < 3 * p) :
    targetR p r ρ 2 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 1) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 1) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 10: `[3/26, 2/17)`, φ̃ = 2. -/
theorem lo_S10 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 3 * p ≤ 26 * r) (hhi : 17 * r < 2 * p) :
    targetR p r ρ 2 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 3) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 2) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 2) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 10: `[3/26, 2/17)`, φ̃ = 2, the terms above `26n`. -/
theorem hi_S10 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 3 * p ≤ 26 * r) (hhi : 17 * r < 2 * p) :
    targetR p r ρ 2 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 1) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 1) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 11: `[2/17, 1/8)`, φ̃ = 1. -/
theorem lo_S11 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 2 * p ≤ 17 * r) (hhi : 8 * r < 1 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 11: `[2/17, 1/8)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S11 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 2 * p ≤ 17 * r) (hhi : 8 * r < 1 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 11: `[2/17, 1/8)`, φ̃ = 1. -/
theorem pol_S11 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 2 * p ≤ 17 * r) (hhi : 8 * r < 1 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 3) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 2) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 2) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 11: `[2/17, 1/8)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S11 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 2 * p ≤ 17 * r) (hhi : 8 * r < 1 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 1) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 2) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 12: `[1/8, 2/15)`, φ̃ = 1. -/
theorem lo_S12 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 8 * r) (hhi : 15 * r < 2 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 12: `[1/8, 2/15)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S12 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 8 * r) (hhi : 15 * r < 2 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 12: `[1/8, 2/15)`, φ̃ = 1. -/
theorem pol_S12 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 8 * r) (hhi : 15 * r < 2 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 3) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 3) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 2) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 12: `[1/8, 2/15)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S12 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 8 * r) (hhi : 15 * r < 2 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 1) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 2) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 13: `[2/15, 3/22)`, φ̃ = 1. -/
theorem lo_S13 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 2 * p ≤ 15 * r) (hhi : 22 * r < 3 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 13: `[2/15, 3/22)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S13 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 2 * p ≤ 15 * r) (hhi : 22 * r < 3 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 13: `[2/15, 3/22)`, φ̃ = 1. -/
theorem pol_S13 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 2 * p ≤ 15 * r) (hhi : 22 * r < 3 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 3) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 3) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 2) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 13: `[2/15, 3/22)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S13 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 2 * p ≤ 15 * r) (hhi : 22 * r < 3 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 2) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 2) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 14: `[3/22, 1/7)`, φ̃ = 1. -/
theorem lo_S14 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 3 * p ≤ 22 * r) (hhi : 7 * r < 1 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 3) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 3) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 3) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 14: `[3/22, 1/7)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S14 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 3 * p ≤ 22 * r) (hhi : 7 * r < 1 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 2) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 2) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 14: `[3/22, 1/7)`, φ̃ = 1. -/
theorem pol_S14 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 3 * p ≤ 22 * r) (hhi : 7 * r < 1 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 3) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 3) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 3) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 14: `[3/22, 1/7)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S14 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 3 * p ≤ 22 * r) (hhi : 7 * r < 1 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 2) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 2) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

set_option maxHeartbeats 2000000 in
/-- stratum 15: `[1/7, 2/13)`, φ̃ = 2. -/
theorem lo_S15 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 7 * r) (hhi : 13 * r < 2 * p) :
    targetR p r ρ 2 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 3) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 3) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 3) (a := 22 * r) hp (by omega) (by omega)
  unfold targetR kumLoR Mres cb
  rw [hR11, hR13, hR9, hR5, hR26, hR24, hR22,
    add_mod_split hR11b (by omega : ρ ≤ p),
    wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 15: `[1/7, 2/13)`, φ̃ = 2, the terms above `26n`. -/
theorem hi_S15 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 7 * r) (hhi : 13 * r < 2 * p) :
    targetR p r ρ 2 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 2) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 2) (a := 17 * r) hp (by omega) (by omega)
  unfold targetR kumHiR Mres cb
  rw [hR11, hR13, hR9, hR5, hR15, hR17,
    add_mod_split hR11b (by omega : ρ ≤ p),
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 15: `[1/7, 2/13)`, φ̃ = 2. -/
theorem pol_S15 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 7 * r) (hhi : 13 * r < 2 * p) :
    2 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 3) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 3) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 3) (a := 22 * r) hp (by omega) (by omega)
  unfold kumLoR Mres cb
  rw [hR11, hR13, hR9, hR5, hR26, hR24, hR22]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 15: `[1/7, 2/13)`, φ̃ = 2, the terms above `26n`. -/
theorem polh_S15 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 7 * r) (hhi : 13 * r < 2 * p) :
    2 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 1) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 2) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 2) (a := 17 * r) hp (by omega) (by omega)
  unfold kumHiR Mres cb
  rw [hR11, hR13, hR9, hR5, hR15, hR17]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
  split_ifs <;> omega

/-- stratum 16: `[2/13, 1/6)`, φ̃ = 1. -/
theorem lo_S16 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 2 * p ≤ 13 * r) (hhi : 6 * r < 1 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 4) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 3) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 3) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 16: `[2/13, 1/6)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S16 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 2 * p ≤ 13 * r) (hhi : 6 * r < 1 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 2) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 2) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 16: `[2/13, 1/6)`, φ̃ = 1. -/
theorem pol_S16 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 2 * p ≤ 13 * r) (hhi : 6 * r < 1 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 4) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 3) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 3) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 16: `[2/13, 1/6)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S16 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 2 * p ≤ 13 * r) (hhi : 6 * r < 1 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 2) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 2) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 17: `[1/6, 3/17)`, φ̃ = 1. -/
theorem lo_S17 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 6 * r) (hhi : 17 * r < 3 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 4) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 4) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 3) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 17: `[1/6, 3/17)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S17 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 6 * r) (hhi : 17 * r < 3 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 2) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 2) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 17: `[1/6, 3/17)`, φ̃ = 1. -/
theorem pol_S17 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 6 * r) (hhi : 17 * r < 3 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 4) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 4) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 3) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 17: `[1/6, 3/17)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S17 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 6 * r) (hhi : 17 * r < 3 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 2) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 2) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 18: `[3/17, 2/11)`, φ̃ = 1. -/
theorem lo_S18 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 3 * p ≤ 17 * r) (hhi : 11 * r < 2 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 4) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 4) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 3) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 18: `[3/17, 2/11)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S18 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 3 * p ≤ 17 * r) (hhi : 11 * r < 2 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 2) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 3) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 18: `[3/17, 2/11)`, φ̃ = 1. -/
theorem pol_S18 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 3 * p ≤ 17 * r) (hhi : 11 * r < 2 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 4) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 4) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 3) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 18: `[3/17, 2/11)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S18 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 3 * p ≤ 17 * r) (hhi : 11 * r < 2 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 1) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 2) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 3) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 19: `[2/11, 5/26)`, φ̃ = 2. -/
theorem lo_S19 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 2 * p ≤ 11 * r) (hhi : 26 * r < 5 * p) :
    targetR p r ρ 2 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 4) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 4) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 4) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 19: `[2/11, 5/26)`, φ̃ = 2, the terms above `26n`. -/
theorem hi_S19 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 2 * p ≤ 11 * r) (hhi : 26 * r < 5 * p) :
    targetR p r ρ 2 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 2) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 3) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

set_option maxHeartbeats 2000000 in
/-- stratum 19: `[2/11, 5/26)`, φ̃ = 2. -/
theorem pol_S19 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 2 * p ≤ 11 * r) (hhi : 26 * r < 5 * p) :
    2 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 4) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 4) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 4) (a := 22 * r) hp (by omega) (by omega)
  unfold kumLoR Mres cb
  rw [hR11, hR13, hR9, hR5, hR26, hR24, hR22]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 19: `[2/11, 5/26)`, φ̃ = 2, the terms above `26n`. -/
theorem polh_S19 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 2 * p ≤ 11 * r) (hhi : 26 * r < 5 * p) :
    2 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 2) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 3) (a := 17 * r) hp (by omega) (by omega)
  unfold kumHiR Mres cb
  rw [hR11, hR13, hR9, hR5, hR15, hR17]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
  split_ifs <;> omega

/-- stratum 20: `[5/26, 1/5)`, φ̃ = 2. -/
theorem lo_S20 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 5 * p ≤ 26 * r) (hhi : 5 * r < 1 * p) :
    targetR p r ρ 2 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 5) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 4) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 4) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 20: `[5/26, 1/5)`, φ̃ = 2, the terms above `26n`. -/
theorem hi_S20 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 5 * p ≤ 26 * r) (hhi : 5 * r < 1 * p) :
    targetR p r ρ 2 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 2) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 3) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

set_option maxHeartbeats 2000000 in
/-- stratum 20: `[5/26, 1/5)`, φ̃ = 2. -/
theorem pol_S20 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 5 * p ≤ 26 * r) (hhi : 5 * r < 1 * p) :
    2 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 5) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 4) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 4) (a := 22 * r) hp (by omega) (by omega)
  unfold kumLoR Mres cb
  rw [hR11, hR13, hR9, hR5, hR26, hR24, hR22]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 20: `[5/26, 1/5)`, φ̃ = 2, the terms above `26n`. -/
theorem polh_S20 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 5 * p ≤ 26 * r) (hhi : 5 * r < 1 * p) :
    2 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 0) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 2) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 3) (a := 17 * r) hp (by omega) (by omega)
  unfold kumHiR Mres cb
  rw [hR11, hR13, hR9, hR5, hR15, hR17]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
  split_ifs <;> omega

/-- stratum 21: `[1/5, 5/24)`, φ̃ = 1. -/
theorem lo_S21 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 5 * r) (hhi : 24 * r < 5 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 21: `[1/5, 5/24)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S21 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 1 * p ≤ 5 * r) (hhi : 24 * r < 5 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 21: `[1/5, 5/24)`, φ̃ = 1. -/
theorem pol_S21 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 5 * r) (hhi : 24 * r < 5 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 1) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 5) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 4) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 4) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 21: `[1/5, 5/24)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S21 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 1 * p ≤ 5 * r) (hhi : 24 * r < 5 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 1) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 3) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 3) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 22: `[5/24, 2/9)`, φ̃ = 1. -/
theorem lo_S22 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 5 * p ≤ 24 * r) (hhi : 9 * r < 2 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 22: `[5/24, 2/9)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S22 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 5 * p ≤ 24 * r) (hhi : 9 * r < 2 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 22: `[5/24, 2/9)`, φ̃ = 1. -/
theorem pol_S22 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 5 * p ≤ 24 * r) (hhi : 9 * r < 2 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 1) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 5) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 5) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 4) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 22: `[5/24, 2/9)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S22 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 5 * p ≤ 24 * r) (hhi : 9 * r < 2 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 1) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 1) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 3) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 3) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 23: `[2/9, 5/22)`, φ̃ = 1. -/
theorem lo_S23 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 2 * p ≤ 9 * r) (hhi : 22 * r < 5 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 23: `[2/9, 5/22)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S23 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 2 * p ≤ 9 * r) (hhi : 22 * r < 5 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 23: `[2/9, 5/22)`, φ̃ = 1. -/
theorem pol_S23 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 2 * p ≤ 9 * r) (hhi : 22 * r < 5 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 2) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 1) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 5) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 5) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 4) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 23: `[2/9, 5/22)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S23 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 2 * p ≤ 9 * r) (hhi : 22 * r < 5 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 2) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 1) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 3) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 3) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 24: `[5/22, 3/13)`, φ̃ = 1. -/
theorem lo_S24 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 5 * p ≤ 22 * r) (hhi : 13 * r < 3 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 2) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 1) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 5) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 5) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 5) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 24: `[5/22, 3/13)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S24 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 5 * p ≤ 22 * r) (hhi : 13 * r < 3 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 2) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 1) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 3) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 3) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 24: `[5/22, 3/13)`, φ̃ = 1. -/
theorem pol_S24 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 5 * p ≤ 22 * r) (hhi : 13 * r < 3 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 2) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 1) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 5) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 5) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 5) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 24: `[5/22, 3/13)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S24 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 5 * p ≤ 22 * r) (hhi : 13 * r < 3 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 2) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 2) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 1) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 3) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 3) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

set_option maxHeartbeats 2000000 in
/-- stratum 25: `[3/13, 4/17)`, φ̃ = 1. -/
theorem lo_S25 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 3 * p ≤ 13 * r) (hhi : 17 * r < 4 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 3) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 2) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 1) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 6) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 5) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 5) (a := 22 * r) hp (by omega) (by omega)
  unfold targetR kumLoR Mres cb
  rw [hR11, hR13, hR9, hR5, hR26, hR24, hR22,
    add_mod_split hR11b (by omega : ρ ≤ p),
    wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 25: `[3/13, 4/17)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S25 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 3 * p ≤ 13 * r) (hhi : 17 * r < 4 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 3) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 2) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 1) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 3) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 3) (a := 17 * r) hp (by omega) (by omega)
  unfold targetR kumHiR Mres cb
  rw [hR11, hR13, hR9, hR5, hR15, hR17,
    add_mod_split hR11b (by omega : ρ ≤ p),
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
  split_ifs <;> omega

/-- stratum 26: `[4/17, 1/4)`, φ̃ = 0. -/
theorem lo_S26 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 4 * p ≤ 17 * r) (hhi : 4 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 26: `[4/17, 1/4)`, φ̃ = 0, the terms above `26n`. -/
theorem hi_S26 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 4 * p ≤ 17 * r) (hhi : 4 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 26: `[4/17, 1/4)`, φ̃ = 0. -/
theorem pol_S26 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 4 * p ≤ 17 * r) (hhi : 4 * r < 1 * p) :
    0 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 3) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 2) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 1) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 6) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 5) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 5) (a := 22 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 26: `[4/17, 1/4)`, φ̃ = 0, the terms above `26n`. -/
theorem polh_S26 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 4 * p ≤ 17 * r) (hhi : 4 * r < 1 * p) :
    0 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 2) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 3) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 2) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 1) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 3) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 4) (a := 17 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

#print axioms Zeta2PtpKumS1.lo_S0
#print axioms Zeta2PtpKumS1.hi_S0
#print axioms Zeta2PtpKumS1.pol_S0
#print axioms Zeta2PtpKumS1.polh_S0
#print axioms Zeta2PtpKumS1.lo_S1
#print axioms Zeta2PtpKumS1.hi_S1
#print axioms Zeta2PtpKumS1.pol_S1
#print axioms Zeta2PtpKumS1.polh_S1
#print axioms Zeta2PtpKumS1.lo_S2
#print axioms Zeta2PtpKumS1.hi_S2
#print axioms Zeta2PtpKumS1.pol_S2
#print axioms Zeta2PtpKumS1.polh_S2
#print axioms Zeta2PtpKumS1.lo_S3
#print axioms Zeta2PtpKumS1.hi_S3
#print axioms Zeta2PtpKumS1.pol_S3
#print axioms Zeta2PtpKumS1.polh_S3
#print axioms Zeta2PtpKumS1.lo_S4
#print axioms Zeta2PtpKumS1.hi_S4
#print axioms Zeta2PtpKumS1.pol_S4
#print axioms Zeta2PtpKumS1.polh_S4
#print axioms Zeta2PtpKumS1.lo_S5
#print axioms Zeta2PtpKumS1.hi_S5
#print axioms Zeta2PtpKumS1.pol_S5
#print axioms Zeta2PtpKumS1.polh_S5
#print axioms Zeta2PtpKumS1.lo_S6
#print axioms Zeta2PtpKumS1.hi_S6
#print axioms Zeta2PtpKumS1.pol_S6
#print axioms Zeta2PtpKumS1.polh_S6
#print axioms Zeta2PtpKumS1.lo_S7
#print axioms Zeta2PtpKumS1.hi_S7
#print axioms Zeta2PtpKumS1.pol_S7
#print axioms Zeta2PtpKumS1.polh_S7
#print axioms Zeta2PtpKumS1.lo_S8
#print axioms Zeta2PtpKumS1.hi_S8
#print axioms Zeta2PtpKumS1.pol_S8
#print axioms Zeta2PtpKumS1.polh_S8
#print axioms Zeta2PtpKumS1.lo_S9
#print axioms Zeta2PtpKumS1.hi_S9
#print axioms Zeta2PtpKumS1.lo_S10
#print axioms Zeta2PtpKumS1.hi_S10
#print axioms Zeta2PtpKumS1.lo_S11
#print axioms Zeta2PtpKumS1.hi_S11
#print axioms Zeta2PtpKumS1.pol_S11
#print axioms Zeta2PtpKumS1.polh_S11
#print axioms Zeta2PtpKumS1.lo_S12
#print axioms Zeta2PtpKumS1.hi_S12
#print axioms Zeta2PtpKumS1.pol_S12
#print axioms Zeta2PtpKumS1.polh_S12
#print axioms Zeta2PtpKumS1.lo_S13
#print axioms Zeta2PtpKumS1.hi_S13
#print axioms Zeta2PtpKumS1.pol_S13
#print axioms Zeta2PtpKumS1.polh_S13
#print axioms Zeta2PtpKumS1.lo_S14
#print axioms Zeta2PtpKumS1.hi_S14
#print axioms Zeta2PtpKumS1.pol_S14
#print axioms Zeta2PtpKumS1.polh_S14
#print axioms Zeta2PtpKumS1.lo_S15
#print axioms Zeta2PtpKumS1.hi_S15
#print axioms Zeta2PtpKumS1.pol_S15
#print axioms Zeta2PtpKumS1.polh_S15
#print axioms Zeta2PtpKumS1.lo_S16
#print axioms Zeta2PtpKumS1.hi_S16
#print axioms Zeta2PtpKumS1.pol_S16
#print axioms Zeta2PtpKumS1.polh_S16
#print axioms Zeta2PtpKumS1.lo_S17
#print axioms Zeta2PtpKumS1.hi_S17
#print axioms Zeta2PtpKumS1.pol_S17
#print axioms Zeta2PtpKumS1.polh_S17
#print axioms Zeta2PtpKumS1.lo_S18
#print axioms Zeta2PtpKumS1.hi_S18
#print axioms Zeta2PtpKumS1.pol_S18
#print axioms Zeta2PtpKumS1.polh_S18
#print axioms Zeta2PtpKumS1.lo_S19
#print axioms Zeta2PtpKumS1.hi_S19
#print axioms Zeta2PtpKumS1.pol_S19
#print axioms Zeta2PtpKumS1.polh_S19
#print axioms Zeta2PtpKumS1.lo_S20
#print axioms Zeta2PtpKumS1.hi_S20
#print axioms Zeta2PtpKumS1.pol_S20
#print axioms Zeta2PtpKumS1.polh_S20
#print axioms Zeta2PtpKumS1.lo_S21
#print axioms Zeta2PtpKumS1.hi_S21
#print axioms Zeta2PtpKumS1.pol_S21
#print axioms Zeta2PtpKumS1.polh_S21
#print axioms Zeta2PtpKumS1.lo_S22
#print axioms Zeta2PtpKumS1.hi_S22
#print axioms Zeta2PtpKumS1.pol_S22
#print axioms Zeta2PtpKumS1.polh_S22
#print axioms Zeta2PtpKumS1.lo_S23
#print axioms Zeta2PtpKumS1.hi_S23
#print axioms Zeta2PtpKumS1.pol_S23
#print axioms Zeta2PtpKumS1.polh_S23
#print axioms Zeta2PtpKumS1.lo_S24
#print axioms Zeta2PtpKumS1.hi_S24
#print axioms Zeta2PtpKumS1.pol_S24
#print axioms Zeta2PtpKumS1.polh_S24
#print axioms Zeta2PtpKumS1.lo_S25
#print axioms Zeta2PtpKumS1.hi_S25
#print axioms Zeta2PtpKumS1.lo_S26
#print axioms Zeta2PtpKumS1.hi_S26
#print axioms Zeta2PtpKumS1.pol_S26
#print axioms Zeta2PtpKumS1.polh_S26

end Zeta2PtpKumS1
