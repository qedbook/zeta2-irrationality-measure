/-
# Row PT-P, layer 12 — `KummerRowsOpen`'s stratum lemmas, shard 4 of 4: strata 81–105

GENERATED by `gen_kummer_lean.py` from `ptp_kummer_strata.py`'s strata; do not edit by hand.
Each lemma quantifies over EVERY `(p, r₀, ρ, u)` with `r₀/p` in its stratum, in the vocabulary
of `Zeta2PtpKumRes` (`targetR`, `kumLoR`, `kumHiR`), and is proved by `residue_spec` with the
stratum's literal floors, the `%` rewrites of `Zeta2PtpKumRes`/`Zeta2PtpCong`, and `omega`.
Nothing here is a valuation bound; `Zeta2Target.zeta2_not_liouvilleWith` is `sorry`.

VINTAGE: toolchain leanprover/lean4:v4.34.0-rc2, mathlib 5aedf732 (current), buildbox.
-/
import Zeta2Defs
import Zeta2Arith
import Zeta2QnInt
import Zeta2PnHarm
import Zeta2Profile
import Zeta2PhiT
import Zeta2NewtonCarry
import Zeta2NewtonAssemble
import Zeta2PnCleared
import Zeta2PnFunc
import Zeta2PnHarmDelta
import Zeta2PhiTDvd
import Zeta2PtpPolar
import Zeta2PtpRun
import Zeta2PtpStratum
import Zeta2PtpCong
import Zeta2PtpPoly
import Zeta2PtpCoeff
import Zeta2HatCarry
import Zeta2PtpKummer
import Zeta2PtpKumRes

namespace Zeta2PtpKumS4

open Zeta2PtpRun Zeta2PtpCong Zeta2PtpKumRes

-- Every stratum lemma carries the SAME binder list so the cover chain can apply it positionally;
-- on a trivial-target stratum `hu`/`hρ1`/`hρ` are then unused.
set_option linter.unusedVariables false

/-- stratum 81: `[13/17, 10/13)`, φ̃ = 2. -/
theorem lo_S81 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 13 * p ≤ 17 * r) (hhi : 13 * r < 10 * p) :
    targetR p r ρ 2 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 9) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 6) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 19) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 18) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 16) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 81: `[13/17, 10/13)`, φ̃ = 2, the terms above `26n`. -/
theorem hi_S81 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 13 * p ≤ 17 * r) (hhi : 13 * r < 10 * p) :
    targetR p r ρ 2 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 9) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 6) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 11) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 13) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

set_option maxHeartbeats 2000000 in
/-- stratum 81: `[13/17, 10/13)`, φ̃ = 2. -/
theorem pol_S81 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 13 * p ≤ 17 * r) (hhi : 13 * r < 10 * p) :
    2 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 9) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 6) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 19) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 18) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 16) (a := 22 * r) hp (by omega) (by omega)
  unfold kumLoR Mres cb
  rw [hR11, hR13, hR9, hR5, hR26, hR24, hR22]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 81: `[13/17, 10/13)`, φ̃ = 2, the terms above `26n`. -/
theorem polh_S81 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 13 * p ≤ 17 * r) (hhi : 13 * r < 10 * p) :
    2 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 9) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 6) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 11) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 13) (a := 17 * r) hp (by omega) (by omega)
  unfold kumHiR Mres cb
  rw [hR11, hR13, hR9, hR5, hR15, hR17]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
  split_ifs <;> omega

/-- stratum 82: `[10/13, 17/22)`, φ̃ = 1. -/
theorem lo_S82 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 10 * p ≤ 13 * r) (hhi : 22 * r < 17 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 82: `[10/13, 17/22)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S82 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 10 * p ≤ 13 * r) (hhi : 22 * r < 17 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 82: `[10/13, 17/22)`, φ̃ = 1. -/
theorem pol_S82 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 10 * p ≤ 13 * r) (hhi : 22 * r < 17 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 6) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 20) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 18) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 16) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 82: `[10/13, 17/22)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S82 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 10 * p ≤ 13 * r) (hhi : 22 * r < 17 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 6) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 11) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 13) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 83: `[17/22, 7/9)`, φ̃ = 1. -/
theorem lo_S83 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 17 * p ≤ 22 * r) (hhi : 9 * r < 7 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 6) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 20) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 18) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 17) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 83: `[17/22, 7/9)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S83 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 17 * p ≤ 22 * r) (hhi : 9 * r < 7 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 6) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 11) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 13) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 83: `[17/22, 7/9)`, φ̃ = 1. -/
theorem pol_S83 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 17 * p ≤ 22 * r) (hhi : 9 * r < 7 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 6) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 20) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 18) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 17) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 83: `[17/22, 7/9)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S83 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 17 * p ≤ 22 * r) (hhi : 9 * r < 7 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 6) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 11) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 13) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 84: `[7/9, 19/24)`, φ̃ = 1. -/
theorem lo_S84 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 7 * p ≤ 9 * r) (hhi : 24 * r < 19 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 20) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 18) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 17) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 84: `[7/9, 19/24)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S84 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 7 * p ≤ 9 * r) (hhi : 24 * r < 19 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 11) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 13) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 84: `[7/9, 19/24)`, φ̃ = 1. -/
theorem pol_S84 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 7 * p ≤ 9 * r) (hhi : 24 * r < 19 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 20) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 18) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 17) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 84: `[7/9, 19/24)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S84 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 7 * p ≤ 9 * r) (hhi : 24 * r < 19 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 11) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 13) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 85: `[19/24, 4/5)`, φ̃ = 1. -/
theorem lo_S85 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 19 * p ≤ 24 * r) (hhi : 5 * r < 4 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 20) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 19) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 17) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 85: `[19/24, 4/5)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S85 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 19 * p ≤ 24 * r) (hhi : 5 * r < 4 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 11) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 13) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 85: `[19/24, 4/5)`, φ̃ = 1. -/
theorem pol_S85 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 19 * p ≤ 24 * r) (hhi : 5 * r < 4 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 20) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 19) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 17) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 85: `[19/24, 4/5)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S85 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 19 * p ≤ 24 * r) (hhi : 5 * r < 4 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 3) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 11) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 13) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 86: `[4/5, 21/26)`, φ̃ = 0. -/
theorem lo_S86 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 4 * p ≤ 5 * r) (hhi : 26 * r < 21 * p) :
    targetR p r ρ 0 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 86: `[4/5, 21/26)`, φ̃ = 0, the terms above `26n`. -/
theorem hi_S86 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 4 * p ≤ 5 * r) (hhi : 26 * r < 21 * p) :
    targetR p r ρ 0 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 86: `[4/5, 21/26)`, φ̃ = 0. -/
theorem pol_S86 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 4 * p ≤ 5 * r) (hhi : 26 * r < 21 * p) :
    0 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 20) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 19) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 17) (a := 22 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 86: `[4/5, 21/26)`, φ̃ = 0, the terms above `26n`. -/
theorem polh_S86 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 4 * p ≤ 5 * r) (hhi : 26 * r < 21 * p) :
    0 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 12) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 13) (a := 17 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 87: `[21/26, 9/11)`, φ̃ = 0. -/
theorem lo_S87 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 21 * p ≤ 26 * r) (hhi : 11 * r < 9 * p) :
    targetR p r ρ 0 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 87: `[21/26, 9/11)`, φ̃ = 0, the terms above `26n`. -/
theorem hi_S87 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 21 * p ≤ 26 * r) (hhi : 11 * r < 9 * p) :
    targetR p r ρ 0 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 87: `[21/26, 9/11)`, φ̃ = 0. -/
theorem pol_S87 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 21 * p ≤ 26 * r) (hhi : 11 * r < 9 * p) :
    0 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 21) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 19) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 17) (a := 22 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 87: `[21/26, 9/11)`, φ̃ = 0, the terms above `26n`. -/
theorem polh_S87 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 21 * p ≤ 26 * r) (hhi : 11 * r < 9 * p) :
    0 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 8) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 12) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 13) (a := 17 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 88: `[9/11, 14/17)`, φ̃ = 2. -/
theorem lo_S88 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 9 * p ≤ 11 * r) (hhi : 17 * r < 14 * p) :
    targetR p r ρ 2 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 21) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 19) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 18) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 88: `[9/11, 14/17)`, φ̃ = 2, the terms above `26n`. -/
theorem hi_S88 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 9 * p ≤ 11 * r) (hhi : 17 * r < 14 * p) :
    targetR p r ρ 2 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 12) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 13) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

set_option maxHeartbeats 2000000 in
/-- stratum 88: `[9/11, 14/17)`, φ̃ = 2. -/
theorem pol_S88 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 9 * p ≤ 11 * r) (hhi : 17 * r < 14 * p) :
    2 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 21) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 19) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 18) (a := 22 * r) hp (by omega) (by omega)
  unfold kumLoR Mres cb
  rw [hR11, hR13, hR9, hR5, hR26, hR24, hR22]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 88: `[9/11, 14/17)`, φ̃ = 2, the terms above `26n`. -/
theorem polh_S88 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 9 * p ≤ 11 * r) (hhi : 17 * r < 14 * p) :
    2 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 12) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 13) (a := 17 * r) hp (by omega) (by omega)
  unfold kumHiR Mres cb
  rw [hR11, hR13, hR9, hR5, hR15, hR17]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
  split_ifs <;> omega

/-- stratum 89: `[14/17, 5/6)`, φ̃ = 1. -/
theorem lo_S89 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 14 * p ≤ 17 * r) (hhi : 6 * r < 5 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 89: `[14/17, 5/6)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S89 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 14 * p ≤ 17 * r) (hhi : 6 * r < 5 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 89: `[14/17, 5/6)`, φ̃ = 1. -/
theorem pol_S89 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 14 * p ≤ 17 * r) (hhi : 6 * r < 5 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 21) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 19) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 18) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 89: `[14/17, 5/6)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S89 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 14 * p ≤ 17 * r) (hhi : 6 * r < 5 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 12) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 14) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 90: `[5/6, 11/13)`, φ̃ = 1. -/
theorem lo_S90 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 5 * p ≤ 6 * r) (hhi : 13 * r < 11 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 90: `[5/6, 11/13)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S90 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 5 * p ≤ 6 * r) (hhi : 13 * r < 11 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 90: `[5/6, 11/13)`, φ̃ = 1. -/
theorem pol_S90 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 5 * p ≤ 6 * r) (hhi : 13 * r < 11 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 21) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 20) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 18) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 90: `[5/6, 11/13)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S90 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 5 * p ≤ 6 * r) (hhi : 13 * r < 11 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 10) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 12) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 14) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 91: `[11/13, 19/22)`, φ̃ = 1. -/
theorem lo_S91 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 11 * p ≤ 13 * r) (hhi : 22 * r < 19 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 91: `[11/13, 19/22)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S91 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 11 * p ≤ 13 * r) (hhi : 22 * r < 19 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

set_option maxHeartbeats 2000000 in
/-- stratum 91: `[11/13, 19/22)`, φ̃ = 1. -/
theorem pol_S91 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 11 * p ≤ 13 * r) (hhi : 22 * r < 19 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 22) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 20) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 18) (a := 22 * r) hp (by omega) (by omega)
  unfold kumLoR Mres cb
  rw [hR11, hR13, hR9, hR5, hR26, hR24, hR22]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 91: `[11/13, 19/22)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S91 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 11 * p ≤ 13 * r) (hhi : 22 * r < 19 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 12) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 14) (a := 17 * r) hp (by omega) (by omega)
  unfold kumHiR Mres cb
  rw [hR11, hR13, hR9, hR5, hR15, hR17]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
  split_ifs <;> omega

/-- stratum 92: `[19/22, 13/15)`, φ̃ = 1. -/
theorem lo_S92 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 19 * p ≤ 22 * r) (hhi : 15 * r < 13 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 22) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 20) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 19) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 92: `[19/22, 13/15)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S92 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 19 * p ≤ 22 * r) (hhi : 15 * r < 13 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 12) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 14) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 92: `[19/22, 13/15)`, φ̃ = 1. -/
theorem pol_S92 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 19 * p ≤ 22 * r) (hhi : 15 * r < 13 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 22) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 20) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 19) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 92: `[19/22, 13/15)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S92 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 19 * p ≤ 22 * r) (hhi : 15 * r < 13 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 12) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 14) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 93: `[13/15, 7/8)`, φ̃ = 1. -/
theorem lo_S93 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 13 * p ≤ 15 * r) (hhi : 8 * r < 7 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 22) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 20) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 19) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 93: `[13/15, 7/8)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S93 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 13 * p ≤ 15 * r) (hhi : 8 * r < 7 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 13) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 14) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 93: `[13/15, 7/8)`, φ̃ = 1. -/
theorem pol_S93 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 13 * p ≤ 15 * r) (hhi : 8 * r < 7 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 22) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 20) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 19) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 93: `[13/15, 7/8)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S93 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 13 * p ≤ 15 * r) (hhi : 8 * r < 7 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 13) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 14) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 94: `[7/8, 15/17)`, φ̃ = 1. -/
theorem lo_S94 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 7 * p ≤ 8 * r) (hhi : 17 * r < 15 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 22) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 21) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 19) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 94: `[7/8, 15/17)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S94 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 7 * p ≤ 8 * r) (hhi : 17 * r < 15 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 13) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 14) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 94: `[7/8, 15/17)`, φ̃ = 1. -/
theorem pol_S94 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 7 * p ≤ 8 * r) (hhi : 17 * r < 15 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 22) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 21) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 19) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 94: `[7/8, 15/17)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S94 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 7 * p ≤ 8 * r) (hhi : 17 * r < 15 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 13) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 14) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 95: `[15/17, 23/26)`, φ̃ = 1. -/
theorem lo_S95 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 15 * p ≤ 17 * r) (hhi : 26 * r < 23 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 22) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 21) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 19) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 95: `[15/17, 23/26)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S95 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 15 * p ≤ 17 * r) (hhi : 26 * r < 23 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 13) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 15) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 95: `[15/17, 23/26)`, φ̃ = 1. -/
theorem pol_S95 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 15 * p ≤ 17 * r) (hhi : 26 * r < 23 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 22) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 21) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 19) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 95: `[15/17, 23/26)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S95 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 15 * p ≤ 17 * r) (hhi : 26 * r < 23 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 13) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 15) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 96: `[23/26, 8/9)`, φ̃ = 1. -/
theorem lo_S96 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 23 * p ≤ 26 * r) (hhi : 9 * r < 8 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 23) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 21) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 19) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 96: `[23/26, 8/9)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S96 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 23 * p ≤ 26 * r) (hhi : 9 * r < 8 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 13) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 15) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 96: `[23/26, 8/9)`, φ̃ = 1. -/
theorem pol_S96 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 23 * p ≤ 26 * r) (hhi : 9 * r < 8 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 23) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 21) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 19) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 96: `[23/26, 8/9)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S96 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 23 * p ≤ 26 * r) (hhi : 9 * r < 8 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 7) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 13) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 15) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 97: `[8/9, 10/11)`, φ̃ = 1. -/
theorem lo_S97 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 8 * p ≤ 9 * r) (hhi : 11 * r < 10 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 23) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 21) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 19) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 97: `[8/9, 10/11)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S97 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 8 * p ≤ 9 * r) (hhi : 11 * r < 10 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 13) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 15) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 97: `[8/9, 10/11)`, φ̃ = 1. -/
theorem pol_S97 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 8 * p ≤ 9 * r) (hhi : 11 * r < 10 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 23) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 21) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 19) (a := 22 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 97: `[8/9, 10/11)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S97 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 8 * p ≤ 9 * r) (hhi : 11 * r < 10 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 9) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 13) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 15) (a := 17 * r) hp (by omega) (by omega)
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 98: `[10/11, 11/12)`, φ̃ = 2. -/
theorem lo_S98 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 10 * p ≤ 11 * r) (hhi : 12 * r < 11 * p) :
    targetR p r ρ 2 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 23) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 21) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 20) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 98: `[10/11, 11/12)`, φ̃ = 2, the terms above `26n`. -/
theorem hi_S98 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 10 * p ≤ 11 * r) (hhi : 12 * r < 11 * p) :
    targetR p r ρ 2 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 13) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 15) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

set_option maxHeartbeats 2000000 in
/-- stratum 98: `[10/11, 11/12)`, φ̃ = 2. -/
theorem pol_S98 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 10 * p ≤ 11 * r) (hhi : 12 * r < 11 * p) :
    2 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 23) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 21) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 20) (a := 22 * r) hp (by omega) (by omega)
  unfold kumLoR Mres cb
  rw [hR11, hR13, hR9, hR5, hR26, hR24, hR22]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 98: `[10/11, 11/12)`, φ̃ = 2, the terms above `26n`. -/
theorem polh_S98 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 10 * p ≤ 11 * r) (hhi : 12 * r < 11 * p) :
    2 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 13) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 15) (a := 17 * r) hp (by omega) (by omega)
  unfold kumHiR Mres cb
  rw [hR11, hR13, hR9, hR5, hR15, hR17]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
  split_ifs <;> omega

/-- stratum 99: `[11/12, 12/13)`, φ̃ = 2. -/
theorem lo_S99 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 11 * p ≤ 12 * r) (hhi : 13 * r < 12 * p) :
    targetR p r ρ 2 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 23) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 22) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 20) (a := 22 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 99: `[11/12, 12/13)`, φ̃ = 2, the terms above `26n`. -/
theorem hi_S99 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 11 * p ≤ 12 * r) (hhi : 13 * r < 12 * p) :
    targetR p r ρ 2 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 13) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 15) (a := 17 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 2 ≤ 2 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  have hK : 1 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

set_option maxHeartbeats 2000000 in
/-- stratum 99: `[11/12, 12/13)`, φ̃ = 2. -/
theorem pol_S99 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 11 * p ≤ 12 * r) (hhi : 13 * r < 12 * p) :
    2 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 23) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 22) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 20) (a := 22 * r) hp (by omega) (by omega)
  unfold kumLoR Mres cb
  rw [hR11, hR13, hR9, hR5, hR26, hR24, hR22]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 99: `[11/12, 12/13)`, φ̃ = 2, the terms above `26n`. -/
theorem polh_S99 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 11 * p ≤ 12 * r) (hhi : 13 * r < 12 * p) :
    2 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 11) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 13) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 15) (a := 17 * r) hp (by omega) (by omega)
  unfold kumHiR Mres cb
  rw [hR11, hR13, hR9, hR5, hR15, hR17]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 100: `[12/13, 14/15)`, φ̃ = 2. -/
theorem lo_S100 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 12 * p ≤ 13 * r) (hhi : 15 * r < 14 * p) :
    targetR p r ρ 2 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 12) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 24) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 22) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 20) (a := 22 * r) hp (by omega) (by omega)
  unfold targetR kumLoR Mres cb
  rw [hR11, hR13, hR9, hR5, hR26, hR24, hR22,
    add_mod_split hR11b (by omega : ρ ≤ p),
    wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 100: `[12/13, 14/15)`, φ̃ = 2, the terms above `26n`. -/
theorem hi_S100 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 12 * p ≤ 13 * r) (hhi : 15 * r < 14 * p) :
    targetR p r ρ 2 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 12) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 13) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 15) (a := 17 * r) hp (by omega) (by omega)
  unfold targetR kumHiR Mres cb
  rw [hR11, hR13, hR9, hR5, hR15, hR17,
    add_mod_split hR11b (by omega : ρ ≤ p),
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
  split_ifs <;> omega

/-- stratum 101: `[14/15, 16/17)`, φ̃ = 1. -/
theorem lo_S101 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 14 * p ≤ 15 * r) (hhi : 17 * r < 16 * p) :
    targetR p r ρ 1 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 101: `[14/15, 16/17)`, φ̃ = 1, the terms above `26n`. -/
theorem hi_S101 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 14 * p ≤ 15 * r) (hhi : 17 * r < 16 * p) :
    targetR p r ρ 1 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 1 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

set_option maxHeartbeats 2000000 in
/-- stratum 101: `[14/15, 16/17)`, φ̃ = 1. -/
theorem pol_S101 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 14 * p ≤ 15 * r) (hhi : 17 * r < 16 * p) :
    1 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 12) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 24) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 22) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 20) (a := 22 * r) hp (by omega) (by omega)
  unfold kumLoR Mres cb
  rw [hR11, hR13, hR9, hR5, hR26, hR24, hR22]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
  split_ifs <;> omega

set_option maxHeartbeats 2000000 in
/-- stratum 101: `[14/15, 16/17)`, φ̃ = 1, the terms above `26n`. -/
theorem polh_S101 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 14 * p ≤ 15 * r) (hhi : 17 * r < 16 * p) :
    1 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 12) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 14) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 15) (a := 17 * r) hp (by omega) (by omega)
  unfold kumHiR Mres cb
  rw [hR11, hR13, hR9, hR5, hR15, hR17]
  simp only [Nat.add_zero, Nat.mod_eq_of_lt hR11b]
  rw [mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
  split_ifs <;> omega

/-- stratum 102: `[16/17, 21/22)`, φ̃ = 0. -/
theorem lo_S102 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 16 * p ≤ 17 * r) (hhi : 22 * r < 21 * p) :
    targetR p r ρ 0 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 102: `[16/17, 21/22)`, φ̃ = 0, the terms above `26n`. -/
theorem hi_S102 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 16 * p ≤ 17 * r) (hhi : 22 * r < 21 * p) :
    targetR p r ρ 0 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 102: `[16/17, 21/22)`, φ̃ = 0. -/
theorem pol_S102 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 16 * p ≤ 17 * r) (hhi : 22 * r < 21 * p) :
    0 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 12) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 24) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 22) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 20) (a := 22 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 102: `[16/17, 21/22)`, φ̃ = 0, the terms above `26n`. -/
theorem polh_S102 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 16 * p ≤ 17 * r) (hhi : 22 * r < 21 * p) :
    0 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 12) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 14) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 16) (a := 17 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 103: `[21/22, 23/24)`, φ̃ = 0. -/
theorem lo_S103 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 21 * p ≤ 22 * r) (hhi : 24 * r < 23 * p) :
    targetR p r ρ 0 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 103: `[21/22, 23/24)`, φ̃ = 0, the terms above `26n`. -/
theorem hi_S103 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 21 * p ≤ 22 * r) (hhi : 24 * r < 23 * p) :
    targetR p r ρ 0 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 103: `[21/22, 23/24)`, φ̃ = 0. -/
theorem pol_S103 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 21 * p ≤ 22 * r) (hhi : 24 * r < 23 * p) :
    0 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 12) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 24) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 22) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 21) (a := 22 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 103: `[21/22, 23/24)`, φ̃ = 0, the terms above `26n`. -/
theorem polh_S103 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 21 * p ≤ 22 * r) (hhi : 24 * r < 23 * p) :
    0 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 12) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 14) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 16) (a := 17 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 104: `[23/24, 25/26)`, φ̃ = 0. -/
theorem lo_S104 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 23 * p ≤ 24 * r) (hhi : 26 * r < 25 * p) :
    targetR p r ρ 0 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 104: `[23/24, 25/26)`, φ̃ = 0, the terms above `26n`. -/
theorem hi_S104 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 23 * p ≤ 24 * r) (hhi : 26 * r < 25 * p) :
    targetR p r ρ 0 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 104: `[23/24, 25/26)`, φ̃ = 0. -/
theorem pol_S104 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 23 * p ≤ 24 * r) (hhi : 26 * r < 25 * p) :
    0 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 12) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 24) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 23) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 21) (a := 22 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 104: `[23/24, 25/26)`, φ̃ = 0, the terms above `26n`. -/
theorem polh_S104 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 23 * p ≤ 24 * r) (hhi : 26 * r < 25 * p) :
    0 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 12) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 14) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 16) (a := 17 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

/-- stratum 105: `[25/26, 1)`, φ̃ = 0. -/
theorem lo_S105 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 25 * p ≤ 26 * r) (hhi : 1 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumLoR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 105: `[25/26, 1)`, φ̃ = 0, the terms above `26n`. -/
theorem hi_S105 {p r ρ u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p) (hρ1 : 1 ≤ ρ) (hρ : ρ < p)
    (hlo : 25 * p ≤ 26 * r) (hhi : 1 * r < 1 * p) :
    targetR p r ρ 0 ≤ kumHiR p r ρ u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  have hT : targetR p r ρ 0 ≤ 1 := by
    unfold targetR
    rw [hR11]
    split_ifs <;> omega
  omega

/-- stratum 105: `[25/26, 1)`, φ̃ = 0. -/
theorem pol_S105 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 25 * p ≤ 26 * r) (hhi : 1 * r < 1 * p) :
    0 + 1 ≤ kumLoR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 12) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R26, hR26, hR26a, hR26b⟩ := residue_spec (q := 25) (a := 26 * r) hp (by omega) (by omega)
  obtain ⟨R24, hR24, hR24a, hR24b⟩ := residue_spec (q := 23) (a := 24 * r) hp (by omega) (by omega)
  obtain ⟨R22, hR22, hR22a, hR22b⟩ := residue_spec (q := 21) (a := 22 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p ((26 * r) % p + p - u) (13 * r) + cb p ((24 * r) % p + p - u) (9 * r)
      + cb p ((22 * r) % p + p - u) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR26, hR24, hR22, wrap_mod hu hR26b, wrap_mod hu hR24b, wrap_mod hu hR22b]
    split_ifs <;> omega
  unfold kumLoR
  omega

/-- stratum 105: `[25/26, 1)`, φ̃ = 0, the terms above `26n`. -/
theorem polh_S105 {p r u : ℕ} (hp : 0 < p) (hr : r < p) (hu : u < p)
    (hlo : 25 * p ≤ 26 * r) (hhi : 1 * r < 1 * p) :
    0 + 1 ≤ kumHiR p r 0 u + 1 := by
  obtain ⟨R11, hR11, hR11a, hR11b⟩ := residue_spec (q := 10) (a := 11 * r) hp (by omega) (by omega)
  obtain ⟨R13, hR13, hR13a, hR13b⟩ := residue_spec (q := 12) (a := 13 * r) hp (by omega) (by omega)
  obtain ⟨R9, hR9, hR9a, hR9b⟩ := residue_spec (q := 8) (a := 9 * r) hp (by omega) (by omega)
  obtain ⟨R5, hR5, hR5a, hR5b⟩ := residue_spec (q := 4) (a := 5 * r) hp (by omega) (by omega)
  obtain ⟨R15, hR15, hR15a, hR15b⟩ := residue_spec (q := 14) (a := 15 * r) hp (by omega) (by omega)
  obtain ⟨R17, hR17, hR17a, hR17b⟩ := residue_spec (q := 16) (a := 17 * r) hp (by omega) (by omega)
  have hK : 0 ≤ cb p (u + p - 1 - (13 * r) % p) (13 * r) + cb p (u + p - 1 - (15 * r) % p) (9 * r)
      + cb p (u + p - 1 - (17 * r) % p) (5 * r) := by
    unfold cb
    rw [hR13, hR9, hR5, hR15, hR17,
    mod_of_lt_two_mul (a := u + p - 1 - R13) (by omega), mod_of_lt_two_mul (a := u + p - 1 - R15) (by omega),
    mod_of_lt_two_mul (a := u + p - 1 - R17) (by omega)]
    split_ifs <;> omega
  unfold kumHiR
  omega

#print axioms Zeta2PtpKumS4.lo_S81
#print axioms Zeta2PtpKumS4.hi_S81
#print axioms Zeta2PtpKumS4.pol_S81
#print axioms Zeta2PtpKumS4.polh_S81
#print axioms Zeta2PtpKumS4.lo_S82
#print axioms Zeta2PtpKumS4.hi_S82
#print axioms Zeta2PtpKumS4.pol_S82
#print axioms Zeta2PtpKumS4.polh_S82
#print axioms Zeta2PtpKumS4.lo_S83
#print axioms Zeta2PtpKumS4.hi_S83
#print axioms Zeta2PtpKumS4.pol_S83
#print axioms Zeta2PtpKumS4.polh_S83
#print axioms Zeta2PtpKumS4.lo_S84
#print axioms Zeta2PtpKumS4.hi_S84
#print axioms Zeta2PtpKumS4.pol_S84
#print axioms Zeta2PtpKumS4.polh_S84
#print axioms Zeta2PtpKumS4.lo_S85
#print axioms Zeta2PtpKumS4.hi_S85
#print axioms Zeta2PtpKumS4.pol_S85
#print axioms Zeta2PtpKumS4.polh_S85
#print axioms Zeta2PtpKumS4.lo_S86
#print axioms Zeta2PtpKumS4.hi_S86
#print axioms Zeta2PtpKumS4.pol_S86
#print axioms Zeta2PtpKumS4.polh_S86
#print axioms Zeta2PtpKumS4.lo_S87
#print axioms Zeta2PtpKumS4.hi_S87
#print axioms Zeta2PtpKumS4.pol_S87
#print axioms Zeta2PtpKumS4.polh_S87
#print axioms Zeta2PtpKumS4.lo_S88
#print axioms Zeta2PtpKumS4.hi_S88
#print axioms Zeta2PtpKumS4.pol_S88
#print axioms Zeta2PtpKumS4.polh_S88
#print axioms Zeta2PtpKumS4.lo_S89
#print axioms Zeta2PtpKumS4.hi_S89
#print axioms Zeta2PtpKumS4.pol_S89
#print axioms Zeta2PtpKumS4.polh_S89
#print axioms Zeta2PtpKumS4.lo_S90
#print axioms Zeta2PtpKumS4.hi_S90
#print axioms Zeta2PtpKumS4.pol_S90
#print axioms Zeta2PtpKumS4.polh_S90
#print axioms Zeta2PtpKumS4.lo_S91
#print axioms Zeta2PtpKumS4.hi_S91
#print axioms Zeta2PtpKumS4.pol_S91
#print axioms Zeta2PtpKumS4.polh_S91
#print axioms Zeta2PtpKumS4.lo_S92
#print axioms Zeta2PtpKumS4.hi_S92
#print axioms Zeta2PtpKumS4.pol_S92
#print axioms Zeta2PtpKumS4.polh_S92
#print axioms Zeta2PtpKumS4.lo_S93
#print axioms Zeta2PtpKumS4.hi_S93
#print axioms Zeta2PtpKumS4.pol_S93
#print axioms Zeta2PtpKumS4.polh_S93
#print axioms Zeta2PtpKumS4.lo_S94
#print axioms Zeta2PtpKumS4.hi_S94
#print axioms Zeta2PtpKumS4.pol_S94
#print axioms Zeta2PtpKumS4.polh_S94
#print axioms Zeta2PtpKumS4.lo_S95
#print axioms Zeta2PtpKumS4.hi_S95
#print axioms Zeta2PtpKumS4.pol_S95
#print axioms Zeta2PtpKumS4.polh_S95
#print axioms Zeta2PtpKumS4.lo_S96
#print axioms Zeta2PtpKumS4.hi_S96
#print axioms Zeta2PtpKumS4.pol_S96
#print axioms Zeta2PtpKumS4.polh_S96
#print axioms Zeta2PtpKumS4.lo_S97
#print axioms Zeta2PtpKumS4.hi_S97
#print axioms Zeta2PtpKumS4.pol_S97
#print axioms Zeta2PtpKumS4.polh_S97
#print axioms Zeta2PtpKumS4.lo_S98
#print axioms Zeta2PtpKumS4.hi_S98
#print axioms Zeta2PtpKumS4.pol_S98
#print axioms Zeta2PtpKumS4.polh_S98
#print axioms Zeta2PtpKumS4.lo_S99
#print axioms Zeta2PtpKumS4.hi_S99
#print axioms Zeta2PtpKumS4.pol_S99
#print axioms Zeta2PtpKumS4.polh_S99
#print axioms Zeta2PtpKumS4.lo_S100
#print axioms Zeta2PtpKumS4.hi_S100
#print axioms Zeta2PtpKumS4.lo_S101
#print axioms Zeta2PtpKumS4.hi_S101
#print axioms Zeta2PtpKumS4.pol_S101
#print axioms Zeta2PtpKumS4.polh_S101
#print axioms Zeta2PtpKumS4.lo_S102
#print axioms Zeta2PtpKumS4.hi_S102
#print axioms Zeta2PtpKumS4.pol_S102
#print axioms Zeta2PtpKumS4.polh_S102
#print axioms Zeta2PtpKumS4.lo_S103
#print axioms Zeta2PtpKumS4.hi_S103
#print axioms Zeta2PtpKumS4.pol_S103
#print axioms Zeta2PtpKumS4.polh_S103
#print axioms Zeta2PtpKumS4.lo_S104
#print axioms Zeta2PtpKumS4.hi_S104
#print axioms Zeta2PtpKumS4.pol_S104
#print axioms Zeta2PtpKumS4.polh_S104
#print axioms Zeta2PtpKumS4.lo_S105
#print axioms Zeta2PtpKumS4.hi_S105
#print axioms Zeta2PtpKumS4.pol_S105
#print axioms Zeta2PtpKumS4.polh_S105

end Zeta2PtpKumS4
